import { component } from "react"

class BookDetails extends component{
    render(){
        return(
            <div>
                BookDetails
            </div>
        )
    }
}

export default BookDetails;